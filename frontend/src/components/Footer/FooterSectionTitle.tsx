import React from "react";

const FooterSectionTitle: React.FC<{title: string}> = ({title}) => <h2 className="text-base text-indigo-600 font-semibold tracking-wide uppercase">{title}</h2>

export default FooterSectionTitle
